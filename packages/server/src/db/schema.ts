/**
 * Drizzle ORM schema definitions for all database tables.
 *
 * @example
 *   import { users, nodes, outlines } from '../db';
 *
 * @consumers services/
 * @depends drizzle-orm
 */

// Schema will be defined in Phase 1
export {};
